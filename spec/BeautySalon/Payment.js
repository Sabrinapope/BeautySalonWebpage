describe ("Payment form", function name(params) {
    //test the form page upon loading

    describe ("when user clicks submit button", function name(params){
    //test thatwhen a user clicks the submit the click submit    
    })

    describe("Payment", function name(params) {
        var Payment;
        BeforeEach(Function() {
            Payment = new Services (); 
    })
    function Payment(services) {
        for(var i = 0; 1 < n; i++) {
            Payment.Services(Chemical/Styling)
        }
    }
    instanceof ("should require 50% deposit before scheduling appointment"); 
})