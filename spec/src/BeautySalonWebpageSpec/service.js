describe("submit payment", function name(params) {
    
    describe ("when the page loads" function name(params) {
        it ("should show payment form", function(){
            expect(submitForm)toBe(true);
        })
    })
    describe("when user clicks the submit button", function name(params) {
        it ("test when a user clicks the submit complete payment", function(){
            expect(PaymentRequest).toBe(true);  
})





try {

}
catch(error){
    
}
